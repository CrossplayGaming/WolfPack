# Reference material — cloned/acquired locally, never committed
reference/

# User-supplied game data — never committed, never shipped
gamedata/*
!gamedata/README.md

# Build outputs
build/
*.pk3

# Tooling noise
__pycache__/
*.pyc

# Engine binaries - never committed (SETUP.bat can download them)
engine/*
!engine/README.md

# Build outputs
dist/
